# Parse Dashboard - لوحة تحكم Parse متقدمة

لوحة تحكم احترافية لإدارة تطبيقات Parse مع واجهة عربية كاملة وميزات متقدمة.

## المميزات الرئيسية

✅ **تسجيل دخول آمن** - نظام مصادقة متقدم مع Parse  
✅ **إدارة المستخدمين** - عرض وتحرير وحذف المستخدمين  
✅ **جدول شامل** - 34 عمود مع إمكانية الإظهار/الإخفاء والترتيب  
✅ **تحرير متقدم** - نموذج تحرير منظم في 8 مجموعات  
✅ **تصدير البيانات** - تصدير إلى CSV  
✅ **واجهة عربية** - دعم كامل للغة العربية  
✅ **تصميم متجاوب** - يعمل على جميع الأجهزة  

## المتطلبات

- **Node.js** v18 أو أحدث
- **npm** أو **pnpm**
- حساب Parse Server مع بيانات الاتصال

## التثبيت والتشغيل

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd parse-dashboard
```

### 2. تثبيت المكتبات
```bash
pnpm install
# أو
npm install
```

### 3. إعداد متغيرات البيئة

أنشئ ملف `.env.local` في جذر المشروع:

```env
# Parse Server Configuration
VITE_PARSE_SERVER_URL=https://pg-app-48f9h01vu8tpthu5cwztzjqbuc1qbg.scalabl.cloud/1/
VITE_PARSE_APP_ID=mbmRIODb8dv6RfslDMXICBySmNJmToTrqcUHE9Mp
VITE_PARSE_JS_KEY=3WlzfL60InCRU3N9ABe2WXFgmJTq9UNAEpNkMcMr

# Database Configuration (إذا كنت تستخدم قاعدة بيانات محلية)
DATABASE_URL=mysql://user:password@localhost:3306/parse_dashboard

# JWT Secret
JWT_SECRET=your-secret-key-here

# OAuth Configuration (اختياري)
VITE_APP_ID=your-app-id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im
```

### 4. تشغيل الخادم المحلي

#### للتطوير:
```bash
pnpm dev
# أو
npm run dev
```

سيفتح الخادم على: `http://localhost:5173`

#### للإنتاج:
```bash
pnpm build
pnpm start
```

## هيكل المشروع

```
parse-dashboard/
├── client/                    # واجهة المستخدم (React + Vite)
│   ├── src/
│   │   ├── pages/            # الصفحات الرئيسية
│   │   │   ├── Login.tsx      # صفحة تسجيل الدخول
│   │   │   ├── Dashboard.tsx  # لوحة التحكم الرئيسية
│   │   │   └── UsersManagement.tsx  # إدارة المستخدمين
│   │   ├── components/       # المكونات المعاد استخدامها
│   │   │   ├── ColumnManager.tsx    # مدير الأعمدة
│   │   │   └── UserEditForm.tsx     # نموذج تحرير المستخدم
│   │   ├── lib/
│   │   │   └── trpc.ts       # عميل tRPC
│   │   └── App.tsx           # التطبيق الرئيسي
│   └── index.html
├── server/                    # الخادم (Express + tRPC)
│   ├── _core/
│   │   ├── parseConfig.ts    # إعدادات Parse
│   │   ├── parseService.ts   # خدمات Parse
│   │   └── ...
│   ├── routers.ts            # مسارات tRPC
│   └── db.ts                 # استعلامات قاعدة البيانات
├── drizzle/                   # إدارة قاعدة البيانات
│   └── schema.ts             # تعريف الجداول
└── package.json
```

## الاستخدام

### تسجيل الدخول
1. افتح التطبيق على `http://localhost:5173`
2. أدخل بيانات اعتماد Parse الخاصة بك
3. اضغط "تسجيل الدخول"

### إدارة المستخدمين
1. انقر على "إدارة المستخدمين" في القائمة الجانبية
2. اعرض جميع المستخدمين في الجدول
3. استخدم الأزرار:
   - ✏️ **تحرير** - لتعديل بيانات المستخدم
   - 👁️ **عرض** - لعرض التفاصيل الكاملة
   - 🗑️ **حذف** - لحذف المستخدم

### إدارة الأعمدة
1. اضغط على زر "الأعمدة" في الأعلى
2. اختر الأعمدة التي تريد عرضها
3. رتب الأعمدة حسب الحاجة
4. اضغط "حفظ التغييرات"

### تصدير البيانات
1. اضغط على زر "تصدير"
2. سيتم تحميل ملف CSV بجميع البيانات المعروضة

## تعديل بيانات Parse

لتغيير بيانات الاتصال بـ Parse:

1. افتح ملف `server/_core/parseConfig.ts`
2. حدّث المتغيرات:
   ```typescript
   const PARSE_SERVER_URL = "your-parse-server-url";
   const PARSE_APP_ID = "your-app-id";
   const PARSE_JS_KEY = "your-js-key";
   ```

## الأوامر المتاحة

```bash
# التطوير
pnpm dev              # تشغيل الخادم والعميل معاً

# البناء
pnpm build            # بناء المشروع للإنتاج

# الإنتاج
pnpm start            # تشغيل الخادم في وضع الإنتاج

# قاعدة البيانات
pnpm db:push          # دفع التغييرات إلى قاعدة البيانات
pnpm db:studio        # فتح واجهة إدارة قاعدة البيانات

# الفحص
pnpm lint             # فحص الأخطاء
pnpm type-check       # فحص أنواع TypeScript
```

## متطلبات Parse Server

تأكد من أن خادم Parse الخاص بك يحتوي على:

- فئة `User` مع الحقول المطلوبة
- مفاتيح الوصول (Application ID, JavaScript Key)
- اتصال آمن (HTTPS)

## استكشاف الأخطاء

### خطأ: "Failed to login"
- تحقق من بيانات الاتصال بـ Parse
- تأكد من صحة Application ID و JavaScript Key
- تحقق من اتصال الإنترنت

### خطأ: "Cannot read properties of undefined"
- تأكد من تثبيت جميع المكتبات: `pnpm install`
- امسح مجلد `node_modules` وأعد التثبيت

### الخادم لا يستجيب
- تحقق من أن الخادم يعمل: `pnpm dev`
- تأكد من استخدام المنفذ الصحيح (5173 للعميل، 3000 للخادم)

## الدعم والمساهمة

للإبلاغ عن الأخطاء أو المساهمة في المشروع، يرجى إنشاء issue أو pull request.

## الترخيص

هذا المشروع مرخص تحت MIT License.

---

**تم تطويره باستخدام:**
- Next.js / React 19
- TypeScript
- Tailwind CSS
- tRPC
- Parse SDK
- Express.js
